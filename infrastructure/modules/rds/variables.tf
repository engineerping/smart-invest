# =============================================================================
# RDS 模块变量定义
# =============================================================================

# -------------------------------------------------------------------
# subnet_ids: RDS 子网组的子网 ID 列表
# 需要至少 2 个子网（可能来自不同可用区）
# 从 VPC 模块传入：[module.vpc.public_subnet_id, module.vpc.private_subnet_id]
# 虽然传入了公有和私有子网，但 RDS 的 publicly_accessible = false 确保不暴露公网
# -------------------------------------------------------------------
variable "subnet_ids" {}

# -------------------------------------------------------------------
# rds_sg_id: RDS 安全组 ID
# 从 VPC 模块传入，该安全组只允许 EC2 安全组的 5432 端口访问
# -------------------------------------------------------------------
variable "rds_sg_id"  {}
