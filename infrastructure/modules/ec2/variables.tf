# =============================================================================
# EC2 模块变量定义
# =============================================================================
# 所有变量都必须由父模块（根 main.tf）传入。
# =============================================================================

# -------------------------------------------------------------------
# public_subnet_id: 公有子网 ID
# EC2 放在公有子网中，获得公网 IP，可以对外提供服务
# -------------------------------------------------------------------
variable "public_subnet_id"     {}

# -------------------------------------------------------------------
# ec2_sg_id: EC2 安全组 ID
# 控制 EC2 实例的网络访问（HTTPS 443 全网 + SSH 22 管理员 IP）
# -------------------------------------------------------------------
variable "ec2_sg_id"            {}

# -------------------------------------------------------------------
# instance_profile_name: IAM 实例配置文件名
# 让 EC2 实例获得访问 AWS 服务（Secrets Manager/SES/CloudWatch）的权限
# -------------------------------------------------------------------
variable "instance_profile_name" {}

# -------------------------------------------------------------------
# key_pair_name: SSH 密钥对名称
# 用于 SSH 登录 EC2（ssh -i <key>.pem ec2-user@<ip>）
# 需要在 AWS Console 中预先创建
# -------------------------------------------------------------------
variable "key_pair_name"       {}

# -------------------------------------------------------------------
# db_secret_arn: Secrets Manager 中数据库密码的 ARN
# UserData 脚本用此 ARN 获取数据库密码
# -------------------------------------------------------------------
variable "db_secret_arn"        {}

# -------------------------------------------------------------------
# region: AWS 区域
# 用于 AWS CLI 命令（--region 参数）
# -------------------------------------------------------------------
variable "region"               {}

# -------------------------------------------------------------------
# app_jar_s3_path: 应用 JAR 包在 S3 上的路径
# 格式如 s3://bucket-name/path/to/app.jar
# UserData 脚本用 aws s3 cp 下载此文件
# -------------------------------------------------------------------
variable "app_jar_s3_path"       {}
