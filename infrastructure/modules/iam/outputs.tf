# =============================================================================
# IAM 模块输出变量
# =============================================================================

# -------------------------------------------------------------------
# instance_profile_name: IAM 实例配置文件名
# 在 EC2 模块中作为 iam_instance_profile 参数传入，
# 让 EC2 实例获得访问 Secrets Manager / SES / CloudWatch 的权限。
# -------------------------------------------------------------------
output "instance_profile_name" { value = aws_iam_instance_profile.app_profile.name }
