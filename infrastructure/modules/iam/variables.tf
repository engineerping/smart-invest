# =============================================================================
# IAM 模块变量定义
# =============================================================================
# 当前模块不需要任何变量输入 —— 所有配置都是硬编码的默认值。
# 如果后续需要根据不同环境（dev/staging/prod）定制权限，
# 可以在这里添加变量来控制策略附加行为。
#
# 例如：
#   variable "environment" {}  → 生产环境附加额外策略
#   variable "extra_policies" {} → 附加额外的自定义策略 ARN 列表
# =============================================================================

# empty —— 此模块当前不需要外部变量
