# =============================================================================
# VPC 模块输出变量
# =============================================================================
# 这些输出被根模块和其他模块引用，实现模块间的依赖传递。
# 父模块通过 module.vpc.<output_name> 来访问这些值。
# =============================================================================

# -------------------------------------------------------------------
# vpc_id: VPC ID（如 vpc-0abcd1234）
# 其他需要 VPC 上下文的资源会用到（如安全组、RDS 子网组）
# -------------------------------------------------------------------
output "vpc_id"            { value = aws_vpc.main.id }

# -------------------------------------------------------------------
# public_subnet_id: 公有子网 ID
# EC2 模块用这个 ID 将实例放到公有子网中
# -------------------------------------------------------------------
output "public_subnet_id"  { value = aws_subnet.public.id }

# -------------------------------------------------------------------
# private_subnet_id: 私有子网 ID
# 用于 RDS 子网组（数据库放在私有子网更安全）
# -------------------------------------------------------------------
output "private_subnet_id" { value = aws_subnet.private.id }

# -------------------------------------------------------------------
# ec2_sg_id: EC2 安全组 ID
# EC2 模块用这个 ID 为实例配置防火墙规则
# -------------------------------------------------------------------
output "ec2_sg_id"        { value = aws_security_group.ec2.id }

# -------------------------------------------------------------------
# rds_sg_id: RDS 安全组 ID
# RDS 模块用这个 ID 为数据库配置防火墙规则（只允许 EC2 访问 5432）
# -------------------------------------------------------------------
output "rds_sg_id"        { value = aws_security_group.rds.id }
