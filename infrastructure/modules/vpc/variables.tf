# =============================================================================
# VPC 模块变量定义
# =============================================================================
# 子模块的变量由父模块在 module block 中传入。
# 这里的变量不需要 default（所有值都由父模块提供），
# 没有 default 的变量 = 必须由调用方提供（required variable）。
# =============================================================================

# -------------------------------------------------------------------
# region: AWS 区域（如 us-east-1）
# 用于拼接可用区名称：${var.region}a → us-east-1a
# AWS 可用区命名规则：<区域代码><字母>，如 us-east-1a, us-east-1b, ...
# 注意：不同账号的 us-east-1a 对应的物理数据中心可能不同
#       （AWS 为了让负载均衡，会随机映射账号到物理 AZ）
# -------------------------------------------------------------------
variable "region"     {}

# -------------------------------------------------------------------
# admin_cidr: 管理员 IP 地址（CIDR 格式）
# 用于 SSH 安全组的白名单规则，只允许指定 IP 访问 22 端口
# 示例值："1.2.3.4/32"（/32 表示精确匹配一个 IP）
# -------------------------------------------------------------------
variable "admin_cidr" {}
